# locations
dir.exports=${basedir}/artifacts
dir.contentbox.exports=${dir.exports}/${contentbox.slug}/${contentbox.version}
dir.build=${basedir}/build/build-contentbox
dir.api=${dir.build}/apidocs

# remote repos for downloading artifacts external dependencies
box.repo.url=https://downloads.ortussolutions.com

# dependencies
dir.resources=${basedir}/build/resources
dir.dependencies=${basedir}/build/dependencies
# Lucee stable for WAR edition
lucee.version=5.3.8.201
# Lucee patching levels if needed
lucee.patch=
tuckey.version=4.0.3
# bleeding edge info
be.exports=${dir.exports}/${contentbox.slug}
